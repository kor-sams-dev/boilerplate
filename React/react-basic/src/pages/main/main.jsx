import React from "react";
import { Link } from "react-router-dom";
const Main = (props) => {
  return (
    <div>
      <Link to="/carousel">Carousel Page</Link>
    </div>
  );
};

export default Main;
